# Week 2 - Structured Data

日期入口：2026-06-17

## 这一周练什么

Week 1 我已经练了：

- 变量
- `if / else`
- 列表
- `for`
- 函数
- `sys.argv`

Week 2 不急着上 FastAPI。

这一周先补一块非常重要、而且和监控项目直接相关的基础：

> 用字典和列表组织“监控目标”这种结构化数据。

## Week 2 目标

到这一周结束时，我希望自己能：

- 看懂一个字典里存了什么
- 用 `key` 取出字典里的值
- 把多个字典放进列表里
- 遍历多个监控目标
- 为后面的 JSON 文件和监控面板打基础

## 这一周为什么重要

后面的 `lenxuan-monitor` 不会只处理一个字符串。

它会处理这种更像真实项目的数据：

- 站点名字
- URL
- 是否检查 HTTPS
- 检查间隔
- 负责人

这些信息很适合先用字典来表示。

## Day 1

文件：

- `scripts/python_week2_day1.py`

今天只做一件事：

- 认识一个“监控目标字典”

今天的任务：

- 运行脚本
- 修改 `name`
- 修改 `url`
- 修改 `check_ssl`
- 修改 `interval_minutes`
- 再加一个新字段，比如 `owner`

## Day 2

文件：

- `scripts/python_week2_day2.py`

重点：

- 认识“多个监控目标组成的列表”

预期结果：

- 能看懂 `monitor_targets` 是“列表里装多个字典”
- 能直接访问第 1 个和第 2 个目标的数据

## Day 3

文件：

- `scripts/python_week2_day3.py`

重点：

- 用 `for` 循环把所有监控目标逐个打印出来

预期结果：

- 新增一个目标后，循环会多打印一组结果

## Day 4

文件：

- `scripts/python_week2_day4.py`

重点：

- 在循环里配合 `if`，只打印满足条件的目标

预期结果：

- 只会看到 `check_ssl` 为 `True` 的目标

## Day 5

文件：

- `scripts/python_week2_day5.py`

重点：

- 先认识一个更像后续 JSON 配置的结构

预期结果：

- 理解为什么一个字典里可以放一个 `targets` 列表

## Day 6-7

复盘重点：

- 字典和列表的区别
- 什么情况下用 `for`
- 监控目标数据是怎么被分组的
- 把其中一段核心结构自己重新敲一遍

## 这周规则

- 先看懂一层，再加下一层
- 先运行，再修改
- 每次只改 1 到 2 个点
- 能用自己的话解释，才算真的学到
