# Week 2 - Structured Data

日期入口：2026-06-17

## 这一周练什么

Week 1 我已经练了：

- 变量
- `if / else`
- 列表
- `for`
- 函数
- `sys.argv`

Week 2 不急着上 FastAPI。

这一周先补一块非常重要、而且和监控项目直接相关的基础：

> 用字典和列表组织“监控目标”这种结构化数据。

## Week 2 目标

到这一周结束时，我希望自己能：

- 看懂一个字典里存了什么
- 用 `key` 取出字典里的值
- 把多个字典放进列表里
- 遍历多个监控目标
- 为后面的 JSON 文件和监控面板打基础

## 这一周为什么重要

后面的 `lenxuan-monitor` 不会只处理一个字符串。

它会处理这种更像真实项目的数据：

- 站点名字
- URL
- 是否检查 HTTPS
- 检查间隔
- 负责人

这些信息很适合先用字典来表示。

## Day 1

文件：

- `scripts/python_week2_day1.py`

今天只做一件事：

- 认识一个“监控目标字典”

今天的任务：

- 运行脚本
- 修改 `name`
- 修改 `url`
- 修改 `check_ssl`
- 修改 `interval_minutes`
- 再加一个新字段，比如 `owner`

## 后面几天的方向

- Day 2：多个监控目标组成列表
- Day 3：循环打印所有监控目标
- Day 4：只挑出启用的目标
- Day 5：为 JSON 文件做准备
- Day 6-7：复盘并把一部分逻辑自己重写

## 这周规则

- 先看懂一层，再加下一层
- 先运行，再修改
- 每次只改 1 到 2 个点
- 能用自己的话解释，才算真的学到
